apiVersion: v1
kind: Service
metadata:
  name: jeasax
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "jeasax"
    port: 8080
  selector:
    app: jeasax
